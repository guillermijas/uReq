- comment = Comment.new
- project = Project.find(params[:project_id])
= simple_form_for([project, @requirement, comment], remote: true) do |f|
  .field
    = f.input :body
  .actions
    = f.button :submit, 'Guardar', class: 'btn btn-primary'

